# hugo-theme-template

A Hugo theme, generated from the scaffold of Hugo 0.165.0.

## Commands

```sh
./c init name=hugo-theme-template   generate the theme, once
./c serve                run the fixture site with live reload
./c conform              build against the scaffold and against this theme, then diff
```
